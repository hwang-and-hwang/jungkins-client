import { style } from '@vanilla-extract/css';

export const root = style({
  padding: '0.5rem 1rem',
  color: 'white',
  background: 'black',
  border: '1px solid white',
  borderRadius: '0.5rem',
});
